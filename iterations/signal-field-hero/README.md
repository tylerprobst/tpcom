# Signal Field Hero (archived iteration)

Typographic particle field — canvas-sampled letter paths, spring-back disruption, point-sprite shader. Archived from `feature/nextjs-village-hero`.

## Key files

```
src/components/SignalHero.tsx
src/components/signal/
  SignalScene.tsx, signalTypography.ts, signalPhysics.ts
  signalShader.ts, signalPointer.ts, signalConstants.ts
```

## To restore

1. Copy `src/components/signal/` and `SignalHero.tsx` into project `src/components/`
2. Set `src/app/page.tsx` to `import SignalHero from "@/components/SignalHero"`

Last archived July 2026.